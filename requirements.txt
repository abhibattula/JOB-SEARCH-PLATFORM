fastapi==0.139.2
uvicorn[standard]==0.51.0
jinja2==3.1.6
python-multipart==0.0.32
httpx==0.28.1
python-jobspy==1.1.82
PyMuPDF==1.28.0
rapidfuzz==3.14.5
pydantic==2.13.4
PyYAML==6.0.3
python-dotenv==1.2.2
openai==2.46.0
pandas==2.3.3
openpyxl==3.1.5
apscheduler==3.11.3
pywebview==6.2.1
plyer==2.1.0
pytest==9.1.1

# Apply Assist (feature 005) — local LLM + browser automation + credential vault.
# llama-cpp-python has no default PyPI prebuilt wheel; install with:
#   pip install -r requirements.txt --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
#
# llama-cpp-python is pinned PER PLATFORM, not to one version, because
# abetlen/llama-cpp-python's macOS arm64 wheel has shipped genuinely
# corrupted (zip CRC-32 failures reproduced independently via direct
# download, not a network/CDN fluke) in several consecutive releases —
# confirmed for 0.3.34/0.3.33/0.3.32/0.3.30/0.3.28/0.3.27; 0.3.34's Windows
# wheel is fine and already verified end-to-end (real inference, real
# frozen build), so Windows stays there. 0.3.29 is the newest macOS wheel
# that verifies zip-clean (0.3.26 also clean, kept as a documented
# fallback if 0.3.29 ever regresses). See specs/005-apply-assist/patch-0.5.5.md.
diskcache==5.6.3
llama-cpp-python==0.3.34; sys_platform == "win32"
llama-cpp-python==0.3.29; sys_platform == "darwin"
playwright==1.61.0
keyring==25.7.0

# Resume builder + tailored PDF export (feature 007) — pure Python, no
# native deps (weasyprint's GTK chain deliberately avoided; see
# specs/007-moat-release/research.md §1).
fpdf2==2.8.7
